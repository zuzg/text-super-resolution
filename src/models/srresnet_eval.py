
from torch.utils.data import DataLoader

# maximize  peak signal-to-noise ratio (PSNR)
# maximize structural similarity (SSIM)

# calculate avg PSNR
# calculate avg SSIM

def eval_model(model, test_loaders:dict[DataLoader]):
    return 